[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "yigthinker-mcp-powerautomate"
version = "0.1.0"
description = "MCP server exposing Power Automate Flow Management tools to Yigthinker"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.13.0,<2",
    "httpx>=0.27.0,<1",
    "pydantic>=2.0.0,<3",
    "msal>=1.23.0,<2",
]

[project.optional-dependencies]
test = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "respx>=0.22.0,<1",
]

[project.scripts]
yigthinker-mcp-powerautomate = "yigthinker_mcp_powerautomate.__main__:main"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
